const ACTIVITY = ['a couch potato', 'active', 'an athlete'];

const AGE = ['young', 'adult', 'senior'];

const DEPENDENCY = ['independent', 'semi-dependent', 'dependent'];

const EXPERIENCE = ['an expert', 'experienced', 'a novice'];

const SEX = ['male', 'female'];

const SIZE = ['small', 'medium', 'large'];

const SOCIAL = ['a wall flower', 'social', 'life of the party'];

const STATUS = ['adopted', 'not adopted'];

const TRAINABILITY = ['class clown', 'attentive student', 'teachers pet'];

const TYPE = ['dog', 'cat', 'no preference'];

module.exports = {
  ACTIVITY,
  AGE,
  DEPENDENCY,
  EXPERIENCE,
  SEX,
  SIZE,
  SOCIAL,
  STATUS,
  TRAINABILITY,
  TYPE
};
